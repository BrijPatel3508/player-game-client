const api = "http://localhost:8080/api";

export const environment = {
    signInApi: `${api}/auth/signin`,
    signUpApi: `${api}/auth/signup`,
    saveGameApi: `${api}/saveGame`,
    getGames: `${api}/getGames`,
    users: `${api}/users`,
}